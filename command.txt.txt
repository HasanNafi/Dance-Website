git config --global user.name "Nafi"

git config --global user.email "work.nafi@gmail.com"

touch .gitignore

git init

git add .

git commit -m "first commit"

git remote add origin git@github.com:HasanNafi/Dance-Website.git

